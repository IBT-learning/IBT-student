apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nginx-ingress
spec:
  rules:
    - host: nginx.info
      http:
        paths:
          - pathType: Prefix
            path: "/nginx"
            backend:
              service:
                name: nginx
                port:
                  number: 80
